import { Auth as AuthView } from '../views/auth'

export default function Auth() {
  return (
    <div className="h-full-screen w-screen bg-bg-gray flex flex-center">
      <AuthView />
    </div>
  )
}
